The "View on Floor Plan" buttons and tile filtering now work for objects placed by any type (via the generic reverse lookup), and generic placements are validated for a registered type and matching location.
